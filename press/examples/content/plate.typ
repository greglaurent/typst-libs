// Content stays pure data: the path to an image under assets/, plus a caption.
#let content = (
  src: "assets/press-mark.svg",
  caption: [The press mark — loaded from this document's assets/ folder.],
)
