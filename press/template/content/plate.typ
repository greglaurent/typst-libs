// This section's content is a dict: the path to an image under assets/, plus a caption.
// Content stays pure data — just the path string; the renderer turns it into an image.
#let content = (
  src: "assets/press-mark.svg",
  caption: [An image resolved from the project's assets/ folder.],
)
